<% flash.each do |key, value| %>
	<%
		if key != 'success' && key != 'error' && key != 'notice'
			key = 'error'
		end
	%>
    <div role="alert" class="alert-message alert-message-<%= key %>">
      <%= value.html_safe %>
    </div>
<% end %>
